name: Readme Update Libraries

on:
  schedule:
    - cron: '0 16 * * *'  # 오전 1시 KST (UTC+9)
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - run: pip install requests

      - run: python .github/scripts/update_libraries.py

      - name: Push
        run: |
          git config --local user.email "ehdgnl8940@naver.com"
          git config --local user.name "shindonghwi"
          git pull --rebase || true
          git diff --quiet || (git add README.md && git commit -m "🔄 Update libraries" && git push)
