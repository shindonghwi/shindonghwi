name: Readme Update Contributions

on:
  schedule:
    - cron: '30 15 * * *'  # 오전 0시 30분 KST (UTC+9)
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - run: pip install requests

      - run: python .github/scripts/update_contributions.py

      - name: Push
        run: |
          git config --local user.email "ehdgnl8940@naver.com"
          git config --local user.name "shindonghwi"
          git pull --rebase || true
          git diff --quiet || (git add README.md .github/data/contributions.json && git commit -m "🔄 Update contributions" && git push)
